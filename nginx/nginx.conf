# nginx/nginx.conf
events {
    worker_connections 1024;
}

http {
    upstream backend {
        server web:8000;
    }

    # Large uploads (bulk photos) + long-running upstream processing
    client_max_body_size 2048m;
    proxy_connect_timeout 300s;
    proxy_send_timeout 3600s;
    proxy_read_timeout 3600s;
    send_timeout 3600s;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;



    server {
        listen 80;
        server_name _;

<<<<<<< HEAD
        # Allow arbitrarily large request bodies so Django file upload limits
        # (already set to sys.maxsize) are the only restriction.  in nginx the
        # default is 1 MB which triggers 413 for big photos.
        client_max_body_size 0;

        # Статика и медиа
        location /static/ {
            alias /app/staticfiles/;
=======
        # Фронтенд (файлы сборки)
        location /assets/ {
            alias /usr/share/nginx/html/assets/;
            expires 1y;
            add_header Cache-Control "public, immutable";
>>>>>>> fedb633 (Update local configuration and fixes)
        }

        # Корень сайта - отдаем index.html (для SPA)
        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
        }

        # Статика Django
        location /static/ {
	  alias /app/staticfiles/;
        }

        # Медиа Django
        location /media/ {
            alias /app/media/;
        }

        # API Django
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_request_buffering off;
        }

        # Админка Django
        location /admin/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_request_buffering off;
        }

        # WebSockets
        location /ws/ {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
